@model Betting.Cms.ViewModels.TournamentsView

<table class="table">
    <tr>
        <th>Name</th>
        <th></th>
    </tr>

    @foreach (var item in Model.List) {
        <tr>
            <td>@item.Name</td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {/* id=item.PrimaryKey */}) |
                @Html.ActionLink("Details", "ViewTournament", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new {/* id=item.PrimaryKey */})
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Create New Tournaments", "CreateTournament")
</p>